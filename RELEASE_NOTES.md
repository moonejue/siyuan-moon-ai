## v1.4.4 — 版本核对与重新发布

本版以用户指定文件夹中的源码重新核对并打包，保留自定义 AI 修复与深灰柔紫界面，统一清单与文档版本。

- 包含思源 v3.8.3 的 `Request body is empty` 修复（JSON 转发编码）。
- 包含自定义 API 路径处理、配置保留和主题界面更新。
- 增加打包后文件一致性检查，防止旧安装包被误发布。

集市索引可能晚于 GitHub Release 更新。如仍显示旧版，可从下方下载 `package.zip`，覆盖安装后重启思源。

验证：冒烟测试、发布校验、ZIP 完整性及源码一致性检查通过；未使用真实供应商密钥联网测试。
